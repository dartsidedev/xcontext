name: xcontext
description: Predictable static extension methods on BuildContext.
version: 0.0.1
homepage: https://github.com/dartsidedev/xcontext
issue_tracker: https://github.com/dartsidedev/xcontext/issues

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
